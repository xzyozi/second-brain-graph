# [PROJECT_KEY-XXXX] タスクタイトル

## 0. メタ情報 (Scope & Impact)
- **影響範囲**: サンドボックス限定 / 本番影響あり（要レビュー） のいずれかを明記
- **既存コードへの依存**: なし（新規ファイルのみ） / あり（既存関数を変更）
- **関連Issue**: なし / #XXXX の後続タスク

## 1. 概要・背景
（タスクの背景や目的、解決すべき課題を記述します）

## 2. 仕様および要求事項
- [ ] 要求事項1
- [ ] 要求事項2

## 3. 段階的実装手順 (Step-by-step Execution)
- **Phase 1 (Core Implementation)**: まず `src/` 配下の本体モジュール・主要ロジック・例外クラスのみを最小変更で優先実装すること。
- **Phase 2 (Test Implementation & Refinement)**: 本体実装完了後、`tests/` 配下の単体テストを作成・修正し、テスト通過を確認すること。

## 4. 編集対象ファイル (Target Files)
- `src/path/to/file.py`（新規作成 / 既存編集）
- `tests/path/to/test_file.py`

## 5. ドメイン知識・技術上の落とし穴 (Domain Knowledge & Technical Pitfalls)

> **このセクションは `create_issue.py` が技術調査フェーズ（Research Phase）で自動生成・追記します。**
> 実装者はここに列挙された仕様上の制約・既知の落とし穴を必ず把握してからコーディングを開始してください。

### 使用ライブラリ・仕様
- **既存ライブラリの利用を優先すること**: 独自に低レベルなバイト列解析・フォーマット判定ロジックを実装する前に、対象ライブラリが同等の機能を標準APIとして提供していないか必ず確認すること（例: `msoffcrypto.OfficeFile.is_encrypted()` 等）。
（使用するライブラリ名、バージョン制約、公式仕様の参照先を記載）

### 典型的な落とし穴 (Pitfalls)
- （例: このライブラリの〇〇APIはXX環境でバグがある）
- （例: ファイルフォーマットの仕様上、△△が複数パターン存在する）

### 推奨実装パターン (Recommended Patterns)
- （例: ◇◇の場合は XX ではなく YY を使うこと）

### 検証用データ・フィクスチャの準備方針
- [ ] テストに必要なサンプルデータ（暗号化ファイル等、LLMが動的生成できない類のもの）は human が事前に `tests/fixtures/` に配置済み / LLMが `conftest.py` で動的生成可能

---

## 6. 設計制約・アンチパターンの禁止 (Architecture Constraints & Forbidden Actions)
安定した品質を維持するため、以下のアーキテクチャ原則を厳守すること：
- **単一責任と一方向のデータフロー**: 関数やクラス間で「相互呼び出し（循環依存・無限再帰）」となる設計を絶対に行わないこと。各機能は単一責任（SRP）を持たせ、処理結果は戻り値（またはストリーム等）として上位の呼び出し元に返し、制御フローを上位に委譲すること。
- **ハック的なフロー制御の禁止**: ループや再帰の停止条件として、不自然なフラグ変数（`is_first_time` 等）や深さカウンターなどの場当たり的な実装（ハック）を用いないこと。
- **堅牢な解析ロジック**: 外部データやファイル構造のパース処理において、特定の名前空間やマジックストリングのみを前提とした脆弱なハードコーディングを行わないこと。標準APIの安全な走査（ワイルドカードやイテレータ等）を利用すること。
- **既存の破壊禁止**: 無関係な設定ファイルやインターフェースを変更しないこと。既存の公開 API シグネチャを破壊しないこと。

## 7. 対象外 (Non-Goals)
- 本Issueでは〇〇の実装は行わない（別Issueで対応予定）
- △△との相互運用は対象外

## 8. 完了定義 (Definition of Done)
- [ ] すべての要求仕様が実装され、責務が適切に分離されたクリーンな設計になっていること。
- [ ] 単体テストがすべて通過すること（例外系のカバレッジも含む）。
- [ ] Ruff の静的解析エラーが一切ないこと。
- [ ] （サンドボックス案件の場合）PR作成は不要、ローカルブランチでのlint/test通過をもって完了とする。
